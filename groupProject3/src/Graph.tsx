import React from 'react'
import NavBar from './NavBar'

const Graph:React.FC=()=>{  //Suppose to have chatroom id ,user id that in room
    return (                       //can text upload photo emoji or something
        <div>
            <NavBar/>
            Graph
        </div>
    )
}

export default Graph
