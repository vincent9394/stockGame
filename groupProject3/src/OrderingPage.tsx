import React from 'react'
import NavBar from './NavBar'

const OrderingPage:React.FC=()=> {
    //const SearchInfo=useSelector((state:IRootState)=>state.stock.SearchInfo); loading searchInfo
    return (
        <div>
             <NavBar/>
            For Ordering
        </div>
    )
}

export default OrderingPage
