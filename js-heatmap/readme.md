`npm install heatmap.js`

```python
python3 -m http.server
```
```zsh mac
lsof -i | grep "(LISTEN)"
```

windows find port
```
netstat -ano | findstr :<PORT>
```

windows kill port
```
taskkill /PID <PID> /F
```